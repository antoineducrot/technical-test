import { type Permission } from './permission.constant'

const readPermissions: Permission[] = ['rw', 'r']

const writePermissions: Permission[] = ['rw', 'w']

export { readPermissions, writePermissions }
